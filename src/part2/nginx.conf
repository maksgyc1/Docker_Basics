worker_processes 1;

events {
    worker_connections 1024;
}

http {
    server {
        listen 80;

        location /status {
            stub_status;
            allow all;
        }

        location / {
            return 200 'Nginx is running\n';
            add_header Content-Type text/plain;
        }
    }
}
